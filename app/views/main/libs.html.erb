<div class="libraries">
<% @libs.each do |l| %>
	<h2><a href="/<%= l.name %>"><%= l.name %></a></h2>
	<div class="lib_link">
		<a href="<%= l.site_url %>"><%= l.site_url %></a>
		<p class="description"><%= l.description %></a>
	</div>
<% end %>
</div>